// Ran the following
// npx prisma
// npx prisma init
//     .... create basic schema here that matches Neon table schema
// npx prisma db pull
// npx prisma generate
// npx prisma studio


generator client {
  provider = "prisma-client-js"
}

datasource neondb {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model playing_with_neon {
  id    Int    @id @default(autoincrement())
  name  String
  value Float? @neondb.Real
}

model test_kill_me {
  id    Int    @id @default(autoincrement())
  name  String
  value Float? @neondb.Real
}
