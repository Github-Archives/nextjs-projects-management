datasource neondb {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model playing_with_neon {
  id    Int    @id @default(autoincrement())
  name  String
  value String
}
